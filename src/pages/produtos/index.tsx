import ProductsPage from "~/components/Entity/Products/Pagina";
import React, { FC, ReactElement } from "react";

const produtos: FC = (): ReactElement => {
  return (
    <>
      <ProductsPage />
    </>
  );
};

export default produtos;
