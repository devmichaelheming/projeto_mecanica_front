import ClientsPage from "~/components/Entity/Clients/Pagina";
import React, { FC, ReactElement } from "react";

const clientes: FC = (): ReactElement => {
  return (
    <>
      <ClientsPage />
    </>
  );
};

export default clientes;
